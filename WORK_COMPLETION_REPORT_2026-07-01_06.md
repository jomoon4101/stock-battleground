# 주식서바이벌 v2 가독성 긴급 수정 작업완료 보고서

- 작업일: 2026-07-01
- 기준 문서: `stock_survival_codex_update_request_v2.md`
- 참고 사이트: Travian 공식 사이트 및 Travian: Legends 공식 블로그의 허브 화면 소개
- 최종 상태: v2 긴급 수정 완료, 자동 테스트 29개 및 배포 빌드 통과

## 1. 이전 작업 미완료 항목 확인

- 이전 통합 요청의 기능 구현은 완료 상태였다.
- 이전 보고서에서 실행하지 못했다고 명시한 390px 정밀 기기 뷰포트 검증을 이번 작업에서 우선 실행했다.
- v2 최신 피드백 기준으로는 기존 라이트 테마 아래에 남아 있던 다크 배경 규칙과 6~11px 크기의 글자가 실질적인 미완료 항목이었다.

## 2. 검정 배경 문제 수정

- 초기 다크 테마와 후속 라이트 테마 규칙을 `legacy` CSS 계층으로 격리했다.
- v2 가독성 계층이 기존 다크 스타일보다 항상 우선하도록 정리했다.
- `html`과 폼 컨트롤의 `color-scheme`을 명시적으로 `light`로 고정했다.
- input, select, option, textarea를 흰 배경과 네이비 텍스트로 통일했다.
- 게임 규칙 카드, 예약 주문, 수량·목표가 입력, 금융 카드, 아이템 카드, 포트폴리오, 쪽지, 게시판, 알림, 탈락 화면의 내부 표면을 흰색 또는 연한 블루·그레이로 지정했다.
- 진한 네이비는 생존 상태바, 하단 메뉴, 서비스 고지처럼 의도한 강조 영역에만 유지했다.

## 3. 글자 및 조작 영역 확대

- 본문 기본 크기를 16px, 보조 문구를 14px 이상으로 정리했다.
- 섹션 제목 24px, 카드 제목 18~20px, 버튼 16px 이상을 기준으로 적용했다.
- 상태바 핵심 수치는 18px, 모바일은 16px로 표시한다.
- 규칙 팝업 설명은 16px/1.6 줄간격, 카드 제목은 18px로 확대했다.
- 예약 주문 입력은 16px, 높이 48px로 확대했다.
- 탭과 일반 버튼은 최소 44~60px 높이로 확장했다.
- 종목 목록, 보유 종목, 정보 카드, 순위, 자산, 로그, 쪽지의 작은 텍스트를 14~16px로 상향했다.
- 글자 크기 조절 범위를 90~125%로 조정해 최소 단계에서도 지나치게 작아지지 않게 했다.

## 4. Travian 참고 반영

- 외부 이미지나 디자인 자산은 복사하지 않았다.
- 큰 행동 버튼, 명확한 카드 경계, 핵심 정보 우선순위, 게임 상태와 행동 영역의 분리 원칙만 주식서바이벌 UI에 맞게 적용했다.
- 현재의 화이트·라이트 블루, 네이비·레드 게임 톤은 유지했다.

## 5. 모바일 수정

- 실제 390×844 뷰포트로 메인 및 게임 화면을 확인했다.
- 메인과 게임 화면 모두 가로 오버플로가 없음을 확인했다.
- 모바일 게임 모드 버튼은 한 줄에 억지로 축소하지 않고 세로 카드로 배치했다.
- 모바일 생존 상태바는 2열 핵심 정보와 전체 폭 `턴 종료` 버튼으로 정리했다.
- 기존 좁은 세로 턴 종료 버튼에서 한글이 한 글자씩 깨지던 문제를 제거했다.
- 모바일 입력 글자 16px, 섹션 제목 20px, 보조 글자 13~14px 기준을 확인했다.

## 6. 실제 브라우저 검증

- 메인 타이틀: 89.6px, 한 줄 표시
- 메인 설명: 17px
- 기능 카드 설명: 14px
- 메인 입력창: 흰 배경, 16px, 48px 높이
- 규칙 모달: 흰 배경
- 규칙 카드: 연한 회색 배경
- 규칙 본문: 16px, 25.6px 줄높이
- 예약 주문 탭: 흰 배경
- 예약 매수 선택, 수량, 목표가 입력: 흰 배경, 16px, 48px 높이
- 390px 모바일: 문서 폭과 뷰포트 폭 일치
- 모바일 턴 종료 버튼: 전체 폭 359px, 글자 17px
- 브라우저 콘솔 오류: 0개

## 7. 자동 검증

- `npm test`: 29개 통과, 실패 0개
- `npm run build`: 성공, `dist` 생성
- `node --check app.js`: 성공
- `node --check engine.js`: 성공
- `node --check server.mjs`: 성공
- `git diff --check`: 오류 없음

## 8. 이번 작업 변경 파일

- `styles.css`: CSS 계층 정리, 전체 라이트 표면 고정, 글자·여백·버튼 확대, 모바일 상태바 개선
- `app.js`: 글자 크기 조절 범위를 90~125%로 변경
- `tests/ui-contract.test.js`: v2 밝은 표면·최소 글자 크기 회귀 테스트 추가
- `WORK_COMPLETION_REPORT_2026-07-01_06.md`: 본 작업완료 보고서

## 9. 미반영 및 대체 사항

- 요청 기능 중 미반영 항목은 없다.
- Travian 메인 주소는 앱 브라우저의 클라이언트 차단으로 직접 열리지 않아, 공식 Travian 검색 결과와 공식 블로그의 새 허브 소개를 보조 자료로 확인했다.
- Travian의 이미지·레이아웃을 복제하는 대신 웹게임 가독성 원칙만 적용했다. 이는 저작물 복제 위험을 피하면서 현재 주식서바이벌의 디자인 정체성을 유지하기 위한 대체 방식이다.
